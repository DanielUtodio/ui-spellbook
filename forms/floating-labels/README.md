## Floating Labels

Floating labels move the input label above the field
when the user focuses or types.

### Why they are used
- Save space compared to static labels
- Keep context visible while typing

### Concepts explored
- CSS-only floating labels
- Focus and input states
- Accessibility considerations
